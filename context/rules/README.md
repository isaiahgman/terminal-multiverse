# Modular Context Rules

This directory contains domain-specific, feature-specific, or task-specific agent context. 
If an agent is working on a specific part of the codebase, it MUST consult the relevant markdown file in this directory.

Global, repository-wide rules remain in the root `AGENTS.md` file.
